import React, {useState} from "react";

const Detail = () => {
    const [link, setLink] = useState(null);

    return (
        <div>
            <h1>Detail Page</h1>
        </div>
    )
}

export default Detail;